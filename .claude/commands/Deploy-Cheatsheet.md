Deploy the cheatsheet to GitHub Pages.

Steps:
1. Stage all changes: `git add -A`
2. Commit with message: "$ARGUMENTS" (if no arguments provided, use "Update cheatsheet")
3. Push to origin master: `git push origin master`
4. Tell the user the deploy is live at: https://joshuaedgecomb-dev.github.io/Cheatsheet/
